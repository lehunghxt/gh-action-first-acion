name: First work flow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest  # Fixed typo: "lastest" -> "latest"
    steps:
      - name: print greeting  # Fixed: no hyphen in step name, added space after -
        run: echo "Hoa ngoc le hung"
      - name: print goodbye  # Fixed: added space after -
        run: | 
          echo "done - bye"
          echo "ccc123"